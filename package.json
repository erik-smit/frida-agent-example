{
  "name": "frida-agent-example",
  "version": "1.0.0",
  "description": "Example Frida agent written in TypeScript",
  "private": true,
  "main": "agent/index.js",
  "scripts": {
    "prepare": "npm run build",
    "build": "frida-compile agent/index.js -o _agent.js",
    "watch": "frida-compile agent/index.js -o _agent.js -w"
  },
  "devDependencies": {
    "@types/frida-gum": "^15.0.0",
    "@types/node": "^13.7.0",
    "frida-compile": "^9.1.0"
  }
}
